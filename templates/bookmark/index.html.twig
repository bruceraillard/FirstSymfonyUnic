{% extends 'base.html.twig' %}

{% block title %}Liste des Marque-Pages{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="text-center text-primary">📌 Liste des Marque-Pages</h1>

        <div class="table-responsive mt-4">
            <table class="table table-bordered table-hover shadow-sm">
                <thead class="table-dark">
                <tr>
                    <th>URL</th>
                    <th>Date de Création</th>
                    <th>Commentaire</th>
                </tr>
                </thead>
                <tbody>
                {% for bookmark in bookmarks %}
                    <tr>
                        <td>
                            <a href="{{ path('bookmark_show', { id: bookmark.id }) }}">{{ bookmark.url }}</a>
                        </td>
                        <td>{{ bookmark.createdAt|date('d/m/Y H:i') }}</td>
                        <td>{{ bookmark.comment ?: 'Aucun commentaire' }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="4" class="text-center text-muted p-3">
                            Aucun marque-page enregistré.
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
